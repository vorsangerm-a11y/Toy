# =============================================================================
# Environment Variables Template
# =============================================================================
# INSTRUCTIONS:
#   1. Copy this file: cp .env.example .env
#   2. Fill in your actual values in .env
#   3. NEVER commit .env — it is in .gitignore
#
# All required variables are listed here with example values.
# =============================================================================

# Application
APP_ENV=development          # development | staging | production
APP_PORT=8000
APP_DEBUG=true               # Set to false in production
APP_SECRET_KEY=change-me-to-a-random-secret-at-least-32-chars

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/toydb
DATABASE_POOL_SIZE=5
DATABASE_POOL_OVERFLOW=10

# Redis (if used for caching/sessions)
REDIS_URL=redis://localhost:6379/0

# Authentication
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRY_HOURS=24

# External APIs (add project-specific keys here)
# STRIPE_SECRET_KEY=sk_test_...
# SENDGRID_API_KEY=SG...
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...
# AWS_REGION=us-east-1

# Logging
LOG_LEVEL=INFO               # DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_FORMAT=json              # json | text

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
